
# config/config.yaml (пример)
# Добавь в свой существующий config или адаптируй под свой проект.

custom:
  scene_path: "scene.yaml"

batch_size:
  Interior: 4096

training:
  max_steps: 20000

optimizer:
  _target_: torch.optim.Adam
  lr: 1.0e-4
